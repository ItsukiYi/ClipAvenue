# ClipAvenue:「AI 切片 → 交付剪映可编辑工程」技术可行性分析

> 基于 D:/OpenMontage 现有实现(clipavenue agent 9 步流水线)与本机剪映环境实测。
> 实测日:2026-09;剪映专业版 11.4.1.14443;草稿目录 `D:/JianyingPro Drafts/`。

---

## 一、你的目标 vs 当前实现

| 你的想法(目标) | 当前实现(clipavenue) | 状态 |
|---|---|---|
| ① AI 收到视频自行语音转文字 | faster-whisper tiny → segments{start,end,text} | ✅ 已有 |
| ② 对文字含义进行分析 | agent 内联:ASR 纠错 + 话题分析 + 切片定位(score/reason) | ✅ 已有,效果还行 |
| ③ 按时间戳执行粗剪 | ffmpeg trim → clips/*.mp4 | ✅ 已有 |
| ④ 按规则执行一部分细剪和字幕校对 | 字幕校对(CorrectionEngine+LLM)、断句(≤17字/行)已完成;**删重复表述、删空档未做** | ⚠️ 部分 |
| ⑤ 交给用户**可编辑项目**(剪映草稿) | 只输出 成片 mp4 + ass + cover.jpg + 文案 json | ❌ 缺口 |

**结论:整个想法只差最后一块(交付剪映可编辑工程),而这一块是全链路里"技术不确定性最高"的一环,值得先做可行性验证再投入。**

---

## 二、本机实测发现(关键事实)

1. **剪映专业版 11.4.1.14443(2026-09-09 更新)**;草稿目录在 `D:\JianyingPro Drafts\`(非默认路径)。
2. **新版草稿是加密/混淆格式**:`draft_content.json` 与 `draft_meta_info.json` 的 Base64 解码结果均为高熵乱码(非明文 JSON)。实测:9月17日、8月6日草稿均如此,`.bak` 备份同为加密。
3. **草稿的逻辑结构 = 标准 JSON(明文模板存在)**:草稿文件夹内的 `template.tmp` 是一份完整明文草稿 JSON(canvas_config / config / materials(40+ 空数组: videos、audios、texts、stickers…) / keyframes / keyframe_graph_list …)。证明剪映"内部用明文 JSON、落盘时套混淆层"。
4. 草稿文件夹内其余文件:`draft.extra`(二进制)、`draft_cover.jpg`(明文图片)、`attachment_editing.json`(明文)。

**推论**:
- "生成剪映草稿"= ①按 template.tmp 结构写明文 JSON → ②套用剪映的混淆/加密层写盘 → ③注册到 root_meta_info.json(或由剪映扫描识别)。
- 加密层不是不可逾越的:社区已有"自动解密加密草稿"的成熟项目(见下节),真正要验证的是 **11.4.1 对程序化生成草稿的兼容性**。

---

## 三、社区生态(不必从零逆向)

GitHub 检索(2026-09)活跃项目:

| 项目 | Stars | 能力 | 与本需求的关系 |
|---|---|---|---|
| [GuanYixuan/pyJianYingDraft](https://github.com/GuanYixuan/pyJianYingDraft) | ~4.4k | Python 生成/导出剪映草稿,"构建全自动化混剪流水线";CapCut 版 pyCapCut 同步开发 | **首选底层库**:把 EditPlan → 剪映草稿 |
| [mcncarl/jianying-headless](https://github.com/mcncarl/jianying-headless) | ~2.4k | 原生剪映草稿、隔离编辑/导出、Agent Skill;2026-09-20 仍在更新 | 与 ClipAvenue agent 架构高度契合 |
| [Hommy-master/capcut-mate](https://github.com/Hommy-master/capcut-mate) | ~1.8k | 剪映 API 生成/下载草稿,扣子插件 | 可研究其草稿生成 API 细节 |
| [renezander030/capcut-cli](https://github.com/renezander030/capcut-cli) | ~0.7k | 直接读写 draft_content.json:字幕、时长、变速、音量、**切长视频为短视频** | 细剪操作(删段/调字幕)可直接参考 |
| [HTWMedia/JyDraft](https://github.com/HTWMedia/JyDraft) | ~90 | **自动解密加密草稿**、云端并发渲染、成片回写草稿目录 | 解密层参考 |
| [zenstory-ai/video-recap-skills](https://github.com/zenstory-ai/video-recap-skills) | — | Claude Code skill:视频→中文解说,可选"导出可编辑剪映草稿" | 你的 skill 化路线的现成参照 |

**注意**:经典老库 OpenJerry/pyJianYingDraft 已不可用(404);上表 GuanYixuan 版是迁移/继任者。

---

## 四、技术重难点清单(按交付"可编辑工程"排序)

### 难点 1:剪映草稿编解码层 ★★★(第一个要打通的点)
- 11.4.1 全加密(content+meta),必须先解密研究结构、再复刻加密写盘;
- template.tmp 明文模板是**结构蓝本**,但真实草稿(带素材/字幕)字段更多:直接在 `D:\JianyingPro Drafts\` 里选一个小草稿用社区库解密,得到"真素材工程"参考;
- 生成工程字段缺失/类型不符 → 剪映可能拒绝打开或静默丢内容,需用社区库做"读回校验"。

### 难点 2:编辑语义映射(架构核心)★★★
- AI 产出的是**编辑操作**(保留段、删除段、字幕行、封面/标题),不是成品视频;
- 必须新增中间表示 **EditPlan JSON**(Op 列表:`keep{start,end}` / `delete{start,end}` / `subtitle{text,t0,t1}` / `cover_image` / `title`…);
- Agent 只输出 EditPlan,"工程生成器"把它翻译成剪映草稿;FFmpeg 渲染降级为快速预览兜底(现状保留)。

### 难点 3:时间轴与音画同步 ★★★
- 剪映时间单位是微秒(µs);segment 由 `source_timerange`(素材内区间)与 `target_timerange`(时间轴位置)拼成;
- **细剪 ≠ 删素材,而是"跳过区间"**:删空档/删重复段后,后续 segment 的 target_timerange 必须整体重算,否则音画错位、字幕对不上;
- 多段 1 小时素材 = materials.videos 多条 + 多条轨,每条 segment 的 source 区间都要按"全局时间戳 → 源素材局部时间戳"换算。

### 难点 4:细剪规则自动化(承接上一轮分析)
- **删重复表达**:LLM 语义判重 → 输出 delete 区间(保留哪一版仍需人工/LLM 决策,属开放问题,可先做"高亮疑似重复段"让用户确认);
- **删空档**:VAD/人声检测,"3 秒没人说话"在直播 BGM 场景需人声分离辅助;
- 字幕校对:CorrectionEngine 已可用,补 LLM 校对 + 修正后时间轴重排即可。

### 难点 5:素材管理与可移植性 ★★
- 剪映草稿引用**本机绝对路径**;素材须固定存放(建议统一放剪映草稿同盘目录),移动/重命名即失效;
- 1 小时×多段的录播动辄数 GB:直接引用原始文件会拖慢剪映索引;建议"工程引用原文件、切段由工程内 segment 表达",避免复制大文件;
- 剪映打开时会重建缓存(.backup、缩略图等),属预期行为,无需干预。

### 难点 6:无法 headless 验证 ★★
- 生成后无法程序化确认"剪映能正常打开";只能人工开剪映看;
- 对策:**分层验证**——先用 pyJianYingDraft 生成迷你工程(1 素材/2 段/2 条字幕),开剪映确认 11.4.1 兼容,再逐级加复杂度;每次剪映升级后回归一次。

### 难点 7:版本升级风险(持续成本)★★
- 剪映 11.x 已加密,格式随时可能调整;社区库是否跟进是长期依赖风险;
- 建议:生产环境**固定剪映版本**,或写"格式健康检查"脚本(生成后用社区库读回,比对关键字段)。

---

## 五、推荐落地路线(分四阶段,每阶段可独立交付)

- **Phase 0(零成本,立即)**:现状保留;新增"剪映素材包"输出——AI 产出的 成片 mp4 + ass + cover + 文案 json 打包目录,用户手动拖入剪映精剪。
- **Phase 1(试点,1-2 天可验证)**:用 pyJianYingDraft 写"最小剪映工程生成器":输入 = 切片时间戳 + 字幕行;输出 = 剪映草稿(写入 `D:\JianyingPro Drafts\AI-{date}` 并注册 root_meta_info.json)。**立刻开剪映验证 11.4.1 兼容性**——这个实验决定整个路线成立与否。
- **Phase 2(核心,推荐优先投入)**:定义 EditPlan 中间表示;agent 第 5 步后新增"生成 EditPlan"步骤,工程生成器将 EditPlan → 剪映草稿(视频轨=切片 keep 区间 + 源素材引用;文本轨=校正后字幕;封面=首帧截图+文案)。
- **Phase 3(细剪规则)**:两个规则执行器——"疑似重复段检测"(LLM 判重,输出 delete 或高亮)与"空档检测"(VAD,输出 delete 区间),并入 Phase 2 的 EditPlan。
- **Phase 4(闭环,远期)**:读取用户修改后的草稿,对比 EditPlan 差异,学习"用户细剪偏好",反哺规则与选题(这也是竞品没有的能力)。

---

## 五·五、Phase 1 实测结果(2026-09-22,已跑通全链路)

在 `D:\OpenMontage\vendor\` 下实际执行了最小验证,**"生成 → 校验 → 修复 → 注册 → 预览"链路的程序化部分全部通过**:

| 步骤 | 工具 | 结果 |
|---|---|---|
| 生成剪映草稿(2 段视频+2 条字幕+跳空档) | pyJianYingDraft(GuanYixuan 版,0.3.x) | ✅ 明文 JSON,结构完整,草稿落在 `D:\JianyingPro Drafts\AI切片测试-20260922` |
| 读回校验 | capcut-cli lint | ✅ 首轮 **errors=0**,有 4 warning + 5 info |
| 自动修复 | `capcut register --materials --apply` + `capcut lint --fix` | ✅ lint **全绿(errors/warnings/info 全 0)**;素材注册进 draft_materials、local_material_id 补链、外部素材 staged 进草稿 assets/、悬空引用清除 |
| 注册进剪映列表 | 脚本写 root_meta_info.json(已备份 .bak-20260922) | ✅ 索引 752→755 条 |
| 无剪映预览 | `capcut render --burn-captions` | ✅ 直接渲染出 22s 预览,两段 concat + 字幕 drawtext 正确 |

**关键实测结论**
1. pyJianYingDraft 生成的是**明文草稿**(声称兼容 10.8;11.4.1 未测,见下);加密只影响"读取已有草稿"(模板模式需 fallback_loader)。
2. capcut-cli 不止能校验,还内置了**细剪规则的现成实现**,可直接映射到 ClipAvenue 的 agent:
   - `detect-silence` → "删除 3 秒空档"规则(输出 keep 段,喂给 cut/compile)
   - `detect-retakes` → "同一意思说三遍只留一遍"规则(文本级近似重复,LCS≥0.8,保留后一次)
   - `compile <spec.json>` → 声明式工程生成器(EditPlan 的现成落地)
   - `render` → headless 预览(解决"生成后无法验证"痛点)
3. 素材路径提示:lint 会把外部素材 staged 进草稿 `assets/video/`,草稿自包含,利于迁移。
4. **人工验证已完成(2026-09-22 16:37)**:用户打开剪映专业版 11.4.1,草稿出现在列表并被成功打开;剪映将明文草稿**就地升级为 11.4.1 原生格式**(`draft_content.json` 变为加密、新增 `Timelines/<draft_id>/`、`timeline_layout.json`、`.locked` 等 11.x 结构)。**至此"AI 切片交付可编辑工程"路线全线验证完毕。**

**能力已冻结为 skill**:`D:\OpenMontage\skills\clipavenue\jianying-draft.md`(v1),并在 `skills\clipavenue\clip-workflow.md` 增加"第 10 步:交付剪映可编辑工程"、在 `skills\INDEX.md` 登记。后续 agent 按 skill 执行 5 步标准链(生成→lint→register+fix→root 注册→render 预览)即可复用。

**Phase 2 实测完成(2026-09-22,EditPlan 流水线)**:"直播→detect-silence(空档检测)→切 keep 片段→EditPlan JSON→`editplan_to_spec.py`→capcut compile→lint→render 预览"全链路跑通。30s 测试直播(3s/5s 两段空档)产出 22.4s 可编辑草稿,3 视频段+3 字幕,compile warnings=0、lint **0/0/0 全绿**、预览字幕精准。**推荐 materialized 模式**(用已切 clip 文件,compile 自动 staged 素材、零修复);**linked 模式多段有已知坑**(同一源文件多段时 capcut-cli 建独立 material 且时长只记本段,剪映 clamp 从头播,多段勿用)。转换器:`D:\OpenMontage\vendor\editplan_to_spec.py`,示例 `editplan_sample_v2.json`。

**产出的脚本/工具(在 `D:\OpenMontage\vendor\`)**:
- `pyJianYingDraft/`(库,v0.3.x)、`capcut-cli/`(库,v0.25.0,node dist/index.js)
- `pyjd-venv/`(Python 3.12 venv,已装 pyJianYingDraft)
- `gen_test_draft.py`(生成草稿)、`check_draft.py`(解析校验)、`register_draft.py`(注册进剪映索引)
- `test_live_source.mp4`(60s 假直播源)、`preview_ai_clip.mp4`(headless 预览成品)

## 六、关键决策建议

1. **目标必须是剪映专业版(PC)**:手机版剪映不开放草稿文件,程序化生成不可行;"导入剪映 app"的流程只有 PC 版成立。
2. **先跑 Phase 1 再谈投入**:加密草稿是最大不确定项,用社区库(建议先试 pyJianYingDraft + capcut-cli 解密参考)做一次最小验证,确认 11.4.1 能打开生成草稿,再投入 Phase 2。
3. **架构上把"AI 判断"与"工程生成"解耦**:Agent 永远只输出 EditPlan/JSON,不直接碰剪映格式;剪映格式变动时只改"工程生成器",不动 agent。
4. **保留 FFmpeg 渲染作为兜底与预览**:AI 切完先出 mp4 供快速检查,确认后再交付草稿工程,避免把"判断失误"直接写进用户工程。
5. **版本锁定 + 回归脚本**:记录剪映版本号;升级前先跑"生成→读回"健康检查。